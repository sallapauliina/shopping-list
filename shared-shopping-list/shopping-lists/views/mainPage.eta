<% layout("./layouts/layout.eta") %>

<h1>Main page</h1>

<h2>Shared shopping lists</h2>

<% if (it.items.length) { %>
<ul>
    <li> 
        Shopping lists: <%= it.lists.length %>
    </li>
     <li> 
        Shopping list items: <%= it.items.length %>
    </li>
</ul>
<% } else { %>
    <h2>No shopping lists yet.</h2>
<% } %>

<h3><a href="/lists">Lists</a></h3>